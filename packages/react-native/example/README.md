# Aura React Native SDK Example

Minimal placeholder to satisfy CI path structure; platform-specific example apps live in their own repositories. This directory can be extended with a full RN example when needed.
